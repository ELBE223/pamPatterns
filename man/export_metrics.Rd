% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_metrics.R, R/reporting_utils.R
\name{export_metrics}
\alias{export_metrics}
\title{Export Metrics to CSV File}
\usage{
export_metrics(metrics_df, file_path, include_metadata = TRUE)

export_metrics(metrics_df, file_path, include_metadata = TRUE)
}
\arguments{
\item{metrics_df}{A `tibble` or `data.frame` containing the metrics to export.}

\item{file_path}{Character string: The desired path for the output CSV file.
The directory will be created if it does not exist.}

\item{include_metadata}{Logical. If `TRUE` (default), adds metadata lines
(e.g., generation date, package version) as comments at the top of the CSV.}
}
\value{
Invisibly returns the `file_path` of the exported file.
  Called for its side effect of writing a file.

Invisibly returns the `file_path` of the exported file.
  Called for its side effect of writing a file.
}
\description{
Writes the calculated (and optionally flagged) metrics data frame to a CSV file.
Can optionally include metadata lines at the beginning of the CSV file,
commented out with '#'.

Writes the calculated (and optionally flagged) metrics data frame to a CSV file.
Can optionally include metadata lines at the beginning of the CSV file,
commented out with '#'.
}
\examples{
\dontrun{
# Assume 'flagged_species_data' is the output from flag_species()
# output_file <- "my_pam_patterns_results.csv"
# export_metrics(flagged_species_data, output_file)
# message("Metrics exported to: ", output_file)
#
# # Export without metadata
# export_metrics(flagged_species_data, "results_no_meta.csv", include_metadata = FALSE)
}
\dontrun{
# Assume 'flagged_species_data' is the output from flag_species()
# output_file <- "my_pam_patterns_results.csv"
# export_metrics(flagged_species_data, output_file)
# message("Metrics exported to: ", output_file)
#
# # Export without metadata
# export_metrics(flagged_species_data, "results_no_meta.csv", include_metadata = FALSE)
}
}
